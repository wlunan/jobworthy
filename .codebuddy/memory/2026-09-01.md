# 2026-09-01

- index.html 搜索功能升级：搜索框内新增字段选择下拉（searchField，可选全部字段/公司/职位/地点/行业/批次/来源），`filterData()` 按所选字段匹配；placeholder 随字段变化（SEARCH_FIELD_PLACEHOLDERS 常量定义在 init 之前，因 restoreViewState 在 init 内被调用）；clearFilters/视图状态保存恢复（saveViewState/restoreViewState）均已包含 searchField；ui.css 新增 .s-select 样式及移动端适配。
- 学历（e）字段整体下线：表格列、eduFilter 筛选、搜索字段选项、CSV 导出、extractEdu()、示例数据均已移除；jobs.json 中残留 e 字段会被忽略。
- 新增数据源体系：常量 DEFAULT_DATA_SOURCE="校招信息聚合平台"；记录带 ds 字段（init 加载/爬虫 mapResult/导入/添加行时补齐），工具栏新增 dsFilter 数据源筛选（暂只含默认数据源，后续接入其他表格岗位数据时在 toolbar 注释处追加 option）；filterData 中 ms3=按 ds 匹配。
- 文档同步更新：docs/SYNC.md 字段表（s 描述改为来源渠道、新增 ds、标注 e 下线）、docs/了解项目.md 字段表、README.md 新增 v2.4.2 亮点+更新日志+徽章、README_EN.md 功能列表；index.html 版本号徽章 v2.3.0→v2.4.2（此前页面落后于 README 的 2.4.1）。
- 教训：并行 replace_in_file 曾出现"成功但实际未落盘"（疑似写入锁超时回滚），批量编辑后必须用 grep 复核关键改动是否真实写入。
